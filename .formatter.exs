# Used by "mix format"
[
  locals_without_parens: [operation: 2, subscription: 2, assert_reply_spec: 3],
  import_deps: [:mneme, :phoenix, :channel_handler],
  inputs: ["{mix,.formatter}.exs", "{config,lib,test}/**/*.{ex,exs}"]
]
